# TODO - Escáner de códigos + agregar productos

- [x] 1) Backend: agregar endpoint `GET /api/products/lookup?value=` buscando por `barcode` y luego por `code`.
- [x] 2) Frontend (Ventas): agregar input con auto-focus + `onKeyDown(Enter)` que llame al endpoint y sume cantidades si el producto ya existe en la venta.
- [x] 3) Frontend (Productos): agregar campo `barcode` al formulario de alta.
- [x] 4) Frontend (Productos): agregar input con auto-búsqueda por escaneo (Enter) usando el endpoint.
- [x] 5) Ajustes menores en estilos/UX y validación rápida (escaneo de ejemplo).

